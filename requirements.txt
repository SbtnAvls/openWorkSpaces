# Windows 11 Workspace Manager Requirements
# Python 3.10+ required

# Core dependencies
pyvda==0.4.0           # Virtual desktop management for Windows
pywin32==307           # Windows API access
psutil==5.9.8          # Process and system information
rich==13.7.1           # Beautiful terminal output

# GUI dependencies
customtkinter==5.2.2   # Modern and customizable tkinter UI library
Pillow>=10.0.0        # Image processing library (for icon extraction)

# Build dependencies
pyinstaller>=6.10.0    # For creating standalone executables (Python 3.13+ compatible)

# Optional but recommended
# If you encounter issues with pyvda, you might need:
# comtypes==1.2.0      # COM interface support (pyvda dependency)